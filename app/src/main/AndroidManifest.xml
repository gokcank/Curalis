<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- USE_EXACT_ALARM kasıtlı olarak kullanılmıyor: Play Console yalnızca çalar saat/takvim
         uygulamalarına otomatik onay veriyor, Curalis bu kategoriye girmiyor. SCHEDULE_EXACT_ALARM
         (kullanıcı onayı gerektiren, herkese açık izin) tek başına yeterli — bkz. AlarmScheduler
         ve AddEditMedicationViewModel'deki izin kontrolü/yönlendirmesi. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".CuralisApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Curalis"
        android:localeConfig="@xml/locales_config"
        tools:targetApi="31">
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <receiver
            android:name=".core.notification.ReminderReceiver"
            android:exported="false" />
            
        <receiver
            android:name=".core.notification.ReminderActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".core.notification.MissedDoseCheckReceiver"
            android:exported="false" />

        <receiver
            android:name=".core.notification.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- FullScreen LockScreen Alarm Activity -->
        <activity
            android:name=".presentation.alarm.AlarmFullScreenActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.Curalis" />

        <!-- Yön kilidi (orientation lock) kullanılmamalıdır. Tam responsive destek. -->
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Curalis">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WorkManager'ın kendi otomatik başlatıcısı kapatılır; görevlerin Hilt bağımlılıklarını
             alabilmesi için WorkManager, CuralisApp'teki Configuration.Provider üzerinden
             (Hilt fabrikasıyla) ihtiyaç anında başlatılır. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
